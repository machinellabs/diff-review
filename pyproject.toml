[project]
name = "diff-review"
version = "0.5.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.25.0",
    "langgraph>=0.1.0",
    "pydantic>=2.0.0",
    "rich>=13.0.0",
]

[project.scripts]
diff-review = "diff_review.cli:main"

[project.optional-dependencies]
openai = [
    "openai>=1.40.0",
]
dev = [
    "pytest>=8.0.0",
    "openai>=1.40.0",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
